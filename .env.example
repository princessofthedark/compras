# Django Settings
SECRET_KEY=cambia-esto-por-una-clave-secreta-segura
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,compras.autodis.mx

# Database
DB_NAME=autodis_compras
DB_USER=postgres
DB_PASSWORD=autodis2026
DB_HOST=db
DB_PORT=5432

# Redis (for Celery)
REDIS_URL=redis://redis:6379/0

# CORS (separar multiples origenes por comas)
CORS_ALLOWED_ORIGINS=http://localhost,http://127.0.0.1

# Email Settings
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.autodis.mx
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=compras@autodis.mx
EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=compras@autodis.mx

# File Upload
MAX_UPLOAD_SIZE=10485760
ALLOWED_FILE_TYPES=pdf

# SSL (False si nginx/proxy maneja SSL)
SECURE_SSL_REDIRECT=False
